query getJeopardyAnswer($question: String) {
    Get {
        JeopardyQuestion(
            nearText: {
                concepts: [$question]
            }
            limit: 10
         ) {
            category
            air_date
            question
            value
            answer
            round
            show_number
            _additional {
                id
                certainty
            }
        }

    }
}