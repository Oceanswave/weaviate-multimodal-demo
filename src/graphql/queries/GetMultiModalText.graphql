query getMultiModalText($searchTerm: String) {
    Get {
        MultiModal(
            nearText: {
                concepts: [$searchTerm]
            }
         ) {
            filename
            image
            _additional {
                id
                certainty
            }
        }
    }
}