query getMultiModalText($searchTerm: String) {
    Get {
        MultiModal(
            nearText: {
                concepts: [$searchTerm]
            }
            limit: 5
         ) {
            filename
            image
            _additional {
                id
                certainty
            }
        }
    }
}