query getMultiModalImage($image: String!) {
    Get {
        MultiModal(
            nearImage: {
                image: $image
            }
            limit: 5
         ) {
            filename
            image
            _additional {
                id
                certainty
            }
        }
    }
}