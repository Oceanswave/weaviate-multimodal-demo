query getMultiModalImage($image: String!) {
    Get {
        MultiModal(
            nearImage: {
                image: $image
            }
         ) {
            filename
            image
            _additional {
                id
                certainty
            }
        }
    }
}